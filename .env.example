# Stride Desktop - Environment Configuration Template

# Local-only mode (no cloud services)
VITE_LOCAL_ONLY=false

# Supabase Configuration
# For local: use http://supabase.stride.local
# For production: use your Supabase project URL
VITE_SUPABASE_URL=http://supabase.stride.local
VITE_SUPABASE_ANON_KEY=(run 'yarn setup' to generate)

# PowerSync Configuration
# For local: use http://powersync.stride.local
# For production: use your PowerSync instance URL
VITE_POWERSYNC_URL=http://powersync.stride.local

# Sentry (Frontend)
VITE_SENTRY_DSN=

# Feature Flags
VITE_ENABLE_SYNC=true
VITE_LOG_LEVEL=debug

# Seed data (used by startup script)
SEED_DATA=minimal

# OAuth - GitHub
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth - Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# SMTP (used by Supabase Auth for sending emails)
SMTP_NO_REPLY_EMAIL=
SMTP_NO_REPLY_PASSWORD=

# PowerSync (must be prefixed with PS_ for powersync.yaml env substitution)
PS_DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:54322/postgres
